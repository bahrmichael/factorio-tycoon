local CONSTANTS = {
    -- Each cell has 6x6 tiles
    CELL_SIZE = 6,
    CITY_GROWTH_TICKS = 60, -- 300,
    CITY_RADIUS = 250,
    -- Minimum ticks until we try adding another city
    EXPANSION_TICKS = 3600,
}

return CONSTANTS